**Build the application**
    docker-compose build

**Run the application**
    docker-compose up -d

**Check the status**
    docker-compose ps

**Launch the application**

On any browser:
    *localhost:5352*
